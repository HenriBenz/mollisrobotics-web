# Copy to .env.local for local development. On Vercel, set these in Project > Settings > Environment Variables.

# Neon Postgres connection string (pooled). Stores early-access and partner form submissions in the `leads` table.
DATABASE_URL=

# Optional: absolute site URL used for metadata, sitemap and canonical links.
NEXT_PUBLIC_SITE_URL=https://mollyrobotics.com
